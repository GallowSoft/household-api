# Supabase Configuration
# Get these values from your Supabase project dashboard: https://supabase.com/dashboard

# Your Supabase project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous/public key (safe for client-side use)
SUPABASE_ANON_KEY=your_anon_key_here

# Your Supabase service role key (NEVER expose this to client-side code)
# This key has admin privileges and should only be used server-side
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: Database connection string (if you need direct database access)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres

# Optional: JWT secret for additional security (if using custom JWT validation)
# JWT_SECRET=your_jwt_secret_here

# Optional: Application port
# PORT=3000

# Optional: Environment indicator
# NODE_ENV=development
